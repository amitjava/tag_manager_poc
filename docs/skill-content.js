const SKILL_DATA = {
  'break-into-tickets':
    '# Break Into Tickets\n\nTake a completed PRD GitHub issue and add a Task checklist to it.\nEach checkbox = one focused unit of work = one Claude window = one PR.\n\n## When to run\n\nAfter `/write-a-prd` (the GitHub issue exists), before starting any `/tdd` session.\n\n## Step 1 — Read the PRD\n\n```\ngh issue view <issue-number>\n```\n\nRead the full issue: Problem Statement, Solution, User Stories, Implementation Decisions, Business Rules Affected.\n\n## Step 2 — Identify ticket boundaries\n\nGood ticket boundaries:\n- One migration or schema change\n- One API endpoint (or a small group of tightly coupled endpoints)\n- One business rule implementation\n- One frontend component or page\n- One integration point (seam)\n\nRules for a well-formed ticket:\n- Can be built and tested independently\n- Has a clear definition of done (tests passing, PR mergeable)\n- Does not require another ticket to be finished before it starts (or explicitly notes the dependency)\n- Fits in one TDD session (a few hours of focused work)\n\nBad boundaries: "backend work", "frontend work", "all the rules" — too broad.\n\n## Step 3 — Map dependencies\n\nFor each ticket, determine whether it depends on another ticket being merged first. A ticket B depends on ticket A if:\n- B calls a function, class, or endpoint created by A\n- B runs a migration that assumes A\'s migration has already run\n- B\'s tests import code that A writes\n\nWrite the dependency graph before writing the task list. Use this format:\n```\nTicket 1: no dependencies\nTicket 2: requires Ticket 1 merged\nTicket 3: requires Ticket 1 + Ticket 2 merged\nTicket 4: requires Ticket 1 merged (independent of Ticket 2 and 3)\n```\n\nTickets with no dependencies on each other CAN run in parallel windows.\nTickets with dependencies MUST run sequentially — the dependency must be merged first.\n\n## Step 4 — Write the task list\n\nUse `gh issue edit` to add a Task section to the PRD issue body:\n\n```\ngh issue edit <issue-number> --body "$(gh issue view <issue-number> --json body -q .body)\n\n## Tasks\n- [ ] Ticket 1: <description> | domain: <name> | no dependencies\n- [ ] Ticket 2: <description> | domain: <name> | requires: Ticket 1 merged\n- [ ] Ticket 3: <description> | domain: <name> | requires: Ticket 1 + 2 merged\n..."\n```\n\nEach ticket description must include:\n- What is being built (e.g. "OrderCalculator service — subtotal, tax, total")\n- Which domain (e.g. "domain: billing")\n- Explicit dependency tag (e.g. "requires: Ticket 1 merged" or "no dependencies")\n\nThe dependency tag is read by the next Claude window when it loads the PRD — it tells it whether to check for merged PRs before starting.\n\n## Step 5 — Confirm to user\n\nList each ticket with its dependency. State:\n> PRD #<N> broken into <X> tickets.\n> Dependency order: [show the graph]\n> Tickets that can run in parallel: [list if any]\n> Start with Ticket 1: <description>\n> Command for each window: "Work on Ticket <N> from PRD #<issue>: <description>"\n',
  'define-seam-contract':
    '# Define Seam Contract\n\nBefore any code crosses a domain boundary, the contract must be written and agreed.\n\n## When to run\n\nDuring SETUP when a feature will cross a seam listed in `CLAUDE.md` with status\n"Not yet established". Do NOT run if the seam is already Active.\n\n## Step 1 — Identify the seam\n\nRead:\n1. `CLAUDE.md` — cross-domain seams table\n2. `backend/src/domains/<provider>/AGENT_CONTEXT.md`\n3. `backend/src/domains/<consumer>/AGENT_CONTEXT.md`\n\nConfirm with the user:\n- Which domain is the **provider** (owns the data/capability)\n- Which domain is the **consumer** (calls the provider)\n- What specific capability is being consumed\n\n## Step 2 — Interview the user\n\nAsk these questions before writing anything. Do not skip.\n\n**Functional:**\n1. What business capability is the consumer getting from the provider?\n2. What happens to the user\'s experience if the provider is unavailable?\n3. Are there business rules only the provider enforces, which the consumer must respect?\n4. Does the consumer store a copy of the data, or always re-fetch?\n\n**Architecture:**\n5. How will the consumer call the provider — HTTP, event/message, shared interface, shared DB?\n6. Who owns versioning? What is the breaking change protocol?\n7. Rate limits or concurrency constraints?\n\n**Data:**\n8. What exact fields cross the boundary? List each one.\n9. For each field: who is the source of truth? Can the consumer cache it?\n10. Any fields the consumer receives but must never display or store?\n\nDo not proceed until all 10 questions have clear answers.\n\n## Step 3 — Create the contract folder and write CONTRACT.md\n\nFolder: `contracts/<provider>-to-<consumer>/`\nUse ASCII only — no Unicode arrows in folder names (they break shell commands).\n\nWrite `contracts/<provider>-to-<consumer>/CONTRACT.md`:\n\n```markdown\n---\nestablished: <today>\nestablished_prd: <PRD issue number if known, else TBD>\nprovider: <domain>\nconsumer: <domain>\n---\n\n# <Provider> → <Consumer> Contract\n\n## Business reason\n<Why does <consumer> need to call <provider>? One paragraph.>\n\n## Capability consumed\n<What specific capability or data the consumer gets.>\n\n## Fields crossing the boundary\n\n| Field | Type | Required | Source of truth | Consumer may cache | Notes |\n|---|---|---|---|---|---|\n| id | string | yes | provider | yes | |\n\n## Fields consumer must NOT store permanently\n<List fields that must always be re-fetched, or "None">\n\n## Fields consumer must NOT display\n<List sensitive fields, or "None">\n\n## Business rules at the boundary\n<Rules the provider enforces that the consumer must not duplicate or override.>\n| Rule | Owner | What consumer must do |\n|---|---|---|\n\n## Degraded state\n<What happens to user experience if the provider is unavailable.>\n- User-visible behavior: ...\n- Consumer fallback: ...\n\n## Integration method\n<HTTP / shared interface / event bus / other>\n\nReference: `backend/src/domains/<provider>/openapi.yaml`\n\n## Versioning\n- Current version: v1\n- Breaking change protocol: <how consumer is notified>\n- Non-breaking additions: allowed with review\n\n## Error handling\n<How the consumer handles provider errors — retry, fallback, surface to user>\n```\n\n## Step 4 — Update CLAUDE.md\n\nUse Edit tool to update the seam\'s status in the Cross-domain seams table:\n\n```\nBefore: | <provider> | <consumer> | <capability> | Not yet established |\nAfter:  | <provider> | <consumer> | <capability> | Active — established <today> |\n```\n\n## Step 5 — Confirm to user\n\nState:\n> Seam contract established: <provider> → <consumer>\n> contracts/<provider>-to-<consumer>/CONTRACT.md written.\n> CLAUDE.md seam status updated to Active.\n> Next step: FEATURE cycle — /grill-me → /update-agent-context → /write-a-prd → /break-into-tickets.\n',
  'design-an-interface':
    '# Design an Interface\n\nBased on "Design It Twice" from "A Philosophy of Software Design": your first idea is unlikely to be the best. Generate multiple radically different designs, then compare.\n\n## Workflow\n\n### 1. Gather Requirements\n\nBefore designing, understand:\n\n- [ ] What problem does this module solve?\n- [ ] Who are the callers? (other modules, external users, tests)\n- [ ] What are the key operations?\n- [ ] Any constraints? (performance, compatibility, existing patterns)\n- [ ] What should be hidden inside vs exposed?\n\nAsk: "What does this module need to do? Who will use it?"\n\n### 2. Generate Designs (Parallel Sub-Agents)\n\nSpawn 3+ sub-agents simultaneously using the Agent tool. Each Agent call runs independently in parallel. Each must produce a **radically different** approach.\n\n```\nPrompt template for each sub-agent:\n\nDesign an interface for: [module description]\n\nRequirements: [gathered requirements]\n\nConstraints for this design: [assign a different constraint to each agent]\n- Agent 1: "Minimize method count - aim for 1-3 methods max"\n- Agent 2: "Maximize flexibility - support many use cases"\n- Agent 3: "Optimize for the most common case"\n- Agent 4: "Take inspiration from [specific paradigm/library]"\n\nOutput format:\n1. Interface signature (types/methods)\n2. Usage example (how caller uses it)\n3. What this design hides internally\n4. Trade-offs of this approach\n```\n\n### 3. Present Designs\n\nShow each design with:\n\n1. **Interface signature** - types, methods, params\n2. **Usage examples** - how callers actually use it in practice\n3. **What it hides** - complexity kept internal\n\nPresent designs sequentially so user can absorb each approach before comparison.\n\n### 4. Compare Designs\n\nAfter showing all designs, compare them on:\n\n- **Interface simplicity**: fewer methods, simpler params\n- **General-purpose vs specialized**: flexibility vs focus\n- **Implementation efficiency**: does shape allow efficient internals?\n- **Depth**: small interface hiding significant complexity (good) vs large interface with thin implementation (bad)\n- **Ease of correct use** vs **ease of misuse**\n\nDiscuss trade-offs in prose, not tables. Highlight where designs diverge most.\n\n### 5. Synthesize\n\nOften the best design combines insights from multiple options. Ask:\n\n- "Which design best fits your primary use case?"\n- "Any elements from other designs worth incorporating?"\n\n## Evaluation Criteria\n\nFrom "A Philosophy of Software Design":\n\n**Interface simplicity**: Fewer methods, simpler params = easier to learn and use correctly.\n\n**General-purpose**: Can handle future use cases without changes. But beware over-generalization.\n\n**Implementation efficiency**: Does interface shape allow efficient implementation? Or force awkward internals?\n\n**Depth**: Small interface hiding significant complexity = deep module (good). Large interface with thin implementation = shallow module (avoid).\n\n## Anti-Patterns\n\n- Don\'t let sub-agents produce similar designs - enforce radical difference\n- Don\'t skip comparison - the value is in contrast\n- Don\'t implement - this is purely about interface shape\n- Don\'t evaluate based on implementation effort\n',
  'design-system-architecture':
    '# Design System Architecture\n\nRun after `/initialize-knowledge-loop` and before the first `/scaffold-domain` call.\n\n## Step 1 — Read existing context\n\nRead `CLAUDE.md`. Note anything already filled in. Do not ask the user to repeat it.\n\n## Step 2 — Identify bounded contexts\n\nAsk the user:\n\n> "Describe each major area of the system that has its own data and rules. Aim for 2–6 domains. For each one, give it a short name and say what it owns."\n\nWait for the full list before proceeding. If the user gives fewer than 2 or more than 6 domains, push back and ask them to reconsider the boundaries.\n\n## Step 3 — Interview for each domain\n\nFor each domain the user named, collect:\n\n1. **Name** — short identifier, no spaces (e.g. `billing`, `identity`, `catalog`)\n2. **Purpose** — one sentence: what is this domain responsible for?\n3. **Data it owns** — what entities or tables live exclusively in this domain\n4. **Owner** — which team or person is accountable\n\nOne round of questions per domain — keep it quick.\n\n## Step 4 — Identify cross-domain seams\n\nAsk the user:\n\n> "Which domains will need to share data or capabilities with each other? For each pair, what does one side need from the other?"\n\nFor each dependency capture:\n- **Provider** — the domain supplying the capability\n- **Consumer** — the domain that needs it\n- **Capability** — one-line description (e.g. "user identity lookup", "payment confirmation event")\n\n## Step 5 — Update CLAUDE.md\n\nUse Edit tool to replace the placeholder rows in the Domains table and Cross-domain seams table with the collected data.\n\nDomains table format:\n```\n| <name> | backend/src/domains/<name>/ | <purpose> | <seam names or —> |\n```\n\nSeams table format:\n```\n| <provider> | <consumer> | <capability> | Not yet established |\n```\n\n## Step 6 — Confirm next steps\n\nTell the user:\n\n> Architecture recorded in CLAUDE.md. Next steps:\n> 1. Run **/scaffold-domain** for each domain listed.\n> 2. Run **/define-seam-contract** for each cross-domain seam before building any feature that crosses a boundary.\n',
  'edit-article':
    '## Step 1 — Parse structure and confirm\n\nDivide the article into sections based on its headings. Treat the content as a directed acyclic graph: some sections depend on concepts introduced in earlier ones. Verify that the section order respects those dependencies — reorder if it does not.\n\nList the sections and their intended purpose. Confirm with the user before proceeding. If the user wants to add, remove, or reorder sections, do it now.\n\n## Step 2 — Rewrite each section\n\nWork through sections in dependency order (foundations before dependents). For each section apply these rules:\n\n- **Prose paragraphs:** Rewrite for clarity, coherence, and forward flow. Hard limit: 240 characters per paragraph. Split anything longer.\n- **Code blocks:** Do not touch. Preserve exactly.\n- **Lists:** Tighten wording — remove filler phrases, make items parallel. Do not change meaning.\n- **Headings:** Check hierarchy (H2 before H3, no skipped levels). Reword if the heading is vague or misleading.\n\nAfter finishing each section, note what changed and why (one line per section).\n\n## Step 3 — Cross-section review\n\nRead the article end-to-end after all sections are rewritten. Check:\n\n1. Does the opening section frame what follows? Does the closing section land the point?\n2. Are there transitions between sections, or do they feel like disconnected chunks?\n3. Is any concept explained more than once? If so, keep the best explanation and cut the rest.\n4. Are there contradictions between sections?\n\nFix any issues found in this pass.\n\n## Step 4 — Show diff and wait for approval\n\nPresent a summary of all changes made, organized by section. For each section: one sentence describing what changed (e.g. "Split 3 long paragraphs, removed redundant intro sentence, tightened list wording"). For structural changes (reordered sections, merged sections), call those out explicitly.\n\nDo not write anything to the file yet. Wait for the user to approve or request adjustments.\n\n## Step 5 — Apply and confirm\n\nOnce the user approves, apply all changes using the Edit tool, section by section. Confirm to the user when done. If any section failed to apply cleanly (e.g. the original text had changed), flag it and show the intended edit so the user can apply it manually.\n',
  'git-guardrails-claude-code':
    '# Setup Git Guardrails\n\nSets up a PreToolUse hook that intercepts and blocks dangerous git commands before Claude executes them.\n\n## What Gets Blocked\n\n- `git push` (all variants including `--force`)\n- `git reset --hard`\n- `git clean -f` / `git clean -fd`\n- `git branch -D`\n- `git checkout .` / `git restore .`\n\nWhen blocked, Claude sees a message telling it that it does not have authority to access these commands.\n\n## Steps\n\n### 1. Ask scope\n\nAsk the user: install for **this project only** (`.claude/settings.json`) or **all projects** (`~/.claude/settings.json`)?\n\n### 2. Copy the hook script\n\nThe hook script is at: `~/.claude/skills/git-guardrails-block-dangerous-git.sh`. Copy it to the project: `cp ~/.claude/skills/git-guardrails-block-dangerous-git.sh .claude/hooks/PreToolUse/block-dangerous-git.sh`\n\nCopy it to the target location based on scope:\n\n- **Project**: `.claude/hooks/block-dangerous-git.sh`\n- **Global**: `~/.claude/hooks/block-dangerous-git.sh`\n\nMake it executable with `chmod +x`.\n\n### 3. Add hook to settings\n\nAdd to the appropriate settings file:\n\n**Project** (`.claude/settings.json`):\n\n```json\n{\n  "hooks": {\n    "PreToolUse": [\n      {\n        "matcher": "Bash",\n        "hooks": [\n          {\n            "type": "command",\n            "command": "\\"$CLAUDE_PROJECT_DIR\\"/.claude/hooks/block-dangerous-git.sh"\n          }\n        ]\n      }\n    ]\n  }\n}\n```\n\n**Global** (`~/.claude/settings.json`):\n\n```json\n{\n  "hooks": {\n    "PreToolUse": [\n      {\n        "matcher": "Bash",\n        "hooks": [\n          {\n            "type": "command",\n            "command": "~/.claude/hooks/block-dangerous-git.sh"\n          }\n        ]\n      }\n    ]\n  }\n}\n```\n\nIf the settings file already exists, merge the hook into existing `hooks.PreToolUse` array — don\'t overwrite other settings.\n\n### 4. Ask about customization\n\nAsk if user wants to add or remove any patterns from the blocked list. Edit the copied script accordingly.\n\n### 5. Verify\n\nRun a quick test:\n\n```bash\necho \'{"tool_input":{"command":"git push origin main"}}\' | <path-to-script>\n```\n\nShould exit with code 2 and print a BLOCKED message to stderr.\n',
  'grill-me':
    'Run a structured discovery interview across the 5 branches below. Work through one\nbranch at a time. For each question, offer your best-guess answer based on what you\nknow — the user corrects or confirms. Do not move to the next branch until the\ncurrent one is resolved.\n\nIf a question can be answered by reading the codebase, read it instead of asking.\n\n## Before starting — read any uploaded docs\n\nIf the user has attached reference documents (specs, designs, existing PRDs, data\nmodels, Figma exports, meeting notes), read them all before asking any questions.\nFor each branch, skip any question that is already clearly answered in those docs —\ntell the user: "Branch N, question X: answered in <doc name> — <one-line summary>."\nOnly ask about genuine gaps.\n\n**No domain file is written.** The output stays in this conversation and is passed directly\nto `/update-agent-context` which will write it to AGENT_CONTEXT.md.\n\n---\n\n## Branch 1 — Scope & Problem\n\n1. What problem does this feature solve, and for whom?\n2. What is explicitly out of scope for this iteration?\n3. What does success look like? Is there a measurable signal?\n4. Is this new, a replacement for something existing, or an extension of an existing flow?\n\n---\n\n## Branch 2 — Use Cases\n\n1. List the primary use cases in priority order. For each: who triggers it, what they want, what the system must do.\n2. What are the edge cases or exception paths? (empty states, bad input, concurrent access)\n3. What use cases are intentionally deferred to a later iteration?\n\n---\n\n## Branch 3 — Workflow Actors\n\nFor every significant workflow in scope:\n\n1. **Who are the actors?** Every human role, system, and potential AI agent. For each: human, automated system, or AI agent?\n2. **What does each actor decide?** For AI agents: what is autonomous vs. handed to a human?\n3. **What triggers the agent?** Event-driven, scheduled, or on-demand?\n4. **Agent failure mode?** Blast radius if it gets it wrong. Who notices, who fixes it?\n5. **What does the agent escalate?** Autonomous (AFK) vs human-in-the-loop (HITL) — draw the line explicitly.\n\n---\n\n## Branch 4 — Dependencies & Constraints\n\n1. Which existing domains, services, or data models does this feature read from or write to?\n2. External APIs, third-party systems, or compliance requirements?\n3. Performance or scale constraints? (latency budget, data volume, concurrency)\n4. Hard dependencies that must exist before this can ship?\n\n---\n\n## Branch 5 — Domain Terms & Open Questions\n\n1. What domain-specific terms came up that need a precise definition?\n   For each term: one-sentence definition.\n   *(These go into the AGENT_CONTEXT.md Glossary section.)*\n2. What decisions are still unresolved and could block design or implementation?\n3. What assumptions need validation?\n4. What risks should be logged in the PRD\'s open questions section?\n\n---\n\n## Save grill-me log\n\nAfter all 5 branches are complete, save the full Q&A transcript before handing off.\n\n1. Determine the PRD brief name — a short slug of the feature (e.g. `discount-and-tax`, `loyalty-earn-restructure`). Ask the user if unclear.\n2. Find the next file number: check `grill-me-docs/<prd-brief-name>/` for existing files (`grill-me-01.md`, `grill-me-02.md`, …). Use the next available number.\n3. Write the transcript to `grill-me-docs/<prd-brief-name>/grill-me-<NN>.md`.\n4. Each file is immutable — never overwrite. A second grill-me session on the same feature appends a new numbered file.\n\nThis folder lives outside any domain folder. It is for ad-hoc human and Claude reference, not part of the domain context loaded per ticket.\n\n## Handoff\n\nAfter the log is saved, hand off to `/update-agent-context`:\n- Pass the glossary terms from Branch 5 explicitly\n- Pass any new business rules surfaced in Branches 1–4\n- Pass the domain name this feature belongs to\n\nThen proceed to `/write-a-prd`.\n',
  'improve-codebase-architecture':
    '# Improve Codebase Architecture\n\nExplore a codebase like an AI would, surface architectural friction, discover opportunities for improving testability, and propose module-deepening refactors as GitHub issue RFCs.\n\nA **deep module** (John Ousterhout, "A Philosophy of Software Design") has a small interface hiding a large implementation. Deep modules are more testable, more AI-navigable, and let you test at the boundary instead of inside.\n\n## Process\n\n### 1. Explore the codebase\n\nUse the Agent tool with subagent_type=Explore to navigate the codebase naturally. Do NOT follow rigid heuristics — explore organically and note where you experience friction:\n\n- Where does understanding one concept require bouncing between many small files?\n- Where are modules so shallow that the interface is nearly as complex as the implementation?\n- Where have pure functions been extracted just for testability, but the real bugs hide in how they\'re called?\n- Where do tightly-coupled modules create integration risk in the seams between them?\n- Which parts of the codebase are untested, or hard to test?\n\nThe friction you encounter IS the signal.\n\n### 2. Present candidates\n\nPresent a numbered list of deepening opportunities. For each candidate, show:\n\n- **Cluster**: Which modules/concepts are involved\n- **Why they\'re coupled**: Shared types, call patterns, co-ownership of a concept\n- **Dependency category**: Dependency categories: (1) Data dependencies — module B reads data produced by A, (2) Control dependencies — A calls B directly, (3) Shared-state dependencies — A and B both write to same store, (4) Temporal dependencies — A must complete before B starts.\n- **Test impact**: What existing tests would be replaced by boundary tests\n\nDo NOT propose interfaces yet. Ask the user: "Which of these would you like to explore?"\n\n### 3. User picks a candidate\n\n### 4. Frame the problem space\n\nBefore spawning sub-agents, write a user-facing explanation of the problem space for the chosen candidate:\n\n- The constraints any new interface would need to satisfy\n- The dependencies it would need to rely on\n- A rough illustrative code sketch to make the constraints concrete — this is not a proposal, just a way to ground the constraints\n\nShow this to the user, then immediately proceed to Step 5. The user reads and thinks about the problem while the sub-agents work in parallel.\n\n### 5. Design multiple interfaces\n\nSpawn 3+ sub-agents in parallel using the Agent tool. Each must produce a **radically different** interface for the deepened module.\n\nPrompt each sub-agent with a separate technical brief (file paths, coupling details, dependency category, what\'s being hidden). This brief is independent of the user-facing explanation in Step 4. Give each agent a different design constraint:\n\n- Agent 1: "Minimize the interface — aim for 1-3 entry points max"\n- Agent 2: "Maximize flexibility — support many use cases and extension"\n- Agent 3: "Optimize for the most common caller — make the default case trivial"\n- Agent 4 (if applicable): "Design around the ports & adapters pattern for cross-boundary dependencies"\n\nEach sub-agent outputs:\n\n1. Interface signature (types, methods, params)\n2. Usage example showing how callers use it\n3. What complexity it hides internally\n4. Dependency strategy (how deps are handled — dependency categories: (1) Data dependencies — module B reads data produced by A, (2) Control dependencies — A calls B directly, (3) Shared-state dependencies — A and B both write to same store, (4) Temporal dependencies — A must complete before B starts.)\n5. Trade-offs\n\nPresent designs sequentially, then compare them in prose.\n\nAfter comparing, give your own recommendation: which design you think is strongest and why. If elements from different designs would combine well, propose a hybrid. Be opinionated — the user wants a strong read, not just a menu.\n\n### 6. User picks an interface (or accepts recommendation)\n\n### 7. Create GitHub issue\n\nCreate a refactor RFC as a GitHub issue using `gh issue create`. Use this template:\n\n```\nTitle: RFC: <short description>\nLabels: rfc, architecture\nBody:\n## Problem\n## Proposed solution\n## Alternatives considered\n## Impact\n## Open questions\n```\n\nDo NOT ask the user to review before creating — just create it and share the URL.\n',
  'initialize-knowledge-loop':
    '# Initialize Knowledge Loop\n\nRun once at the very start of a new project, before any domain work or scaffold-domain calls.\n\n## Step 1 — Gather project details\n\nAsk the user these four questions before doing anything else:\n\n1. **Project name** — what is this project called?\n2. **One-line purpose** — what does this system do? (One sentence.)\n3. **Team size** — how many people will be working on this?\n4. **Tech stack** — what runtime and database will be used?\n\nCollect all four answers before proceeding.\n\n## Step 2 — Create CLAUDE.md at project root\n\nWrite `CLAUDE.md` with the following content:\n\n```markdown\n# <Project Name>\n\n<One-line purpose>\n\n**Stack:** <runtime> / <database>\n**Team:** <size>\n\n---\n\n## Context loading\n\nWhen starting any feature ticket:\n1. Read the GitHub issue (`gh issue view <N>`)\n2. Identify the domain from the issue\n3. Read `backend/src/domains/<name>/AGENT_CONTEXT.md`\n4. Read `backend/src/domains/<name>/domain-rules.yaml`\n5. Read `contracts/<provider>-to-<consumer>/CONTRACT.md` if the ticket touches a cross-domain seam\n\n---\n\n## Domains\n\n| Domain | Path | Purpose | Seams |\n|---|---|---|---|\n| _populated by /design-system-architecture_ | | | |\n\n---\n\n## Cross-domain seams\n\n| Provider | Consumer | Capability | Status |\n|---|---|---|---|\n| _populated by /design-system-architecture_ | | | |\n\n---\n\n## Conventions\n\n- Rule IDs: `RULE-<DOMAIN>-<NNN>` (3-digit zero-padded integer, e.g. RULE-CATALOG-001)\n- Contract folder: `contracts/<provider>-to-<consumer>/CONTRACT.md`\n- Domain files: `backend/src/domains/<name>/` (AGENT_CONTEXT.md, domain-rules.yaml, openapi.yaml)\n```\n\n## Step 3 — Initial git commit\n\n```\ngit add CLAUDE.md\ngit commit -m "Init: knowledge-loop scaffold"\n```\n\n## Step 4 — Confirm next steps\n\nTell the user:\n\n> Knowledge-loop is ready. Next steps:\n> 1. Run **/design-system-architecture** to define your domains and cross-domain seams.\n> 2. Run **/scaffold-domain** for each domain.\n> 3. Run **/define-seam-contract** for any cross-domain seams before the first feature that crosses a boundary.\n> 4. Run **/setup-pre-commit**, **/git-guardrails-claude-code**, **/setup-spec-ci** to install project guardrails.\n',
  'migrate-to-shoehorn':
    '# Migrate to Shoehorn\n\n## Why shoehorn?\n\n`shoehorn` lets you pass partial data in tests while keeping TypeScript happy. It replaces `as` assertions with type-safe alternatives.\n\n**Test code only.** Never use shoehorn in production code.\n\nProblems with `as` in tests:\n\n- Trained not to use it\n- Must manually specify target type\n- Double-as (`as unknown as Type`) for intentionally wrong data\n\n## Install\n\n```bash\nnpm i @total-typescript/shoehorn\n```\n\n## Migration patterns\n\n### Large objects with few needed properties\n\nBefore:\n\n```ts\ntype Request = {\n  body: { id: string };\n  headers: Record<string, string>;\n  cookies: Record<string, string>;\n  // ...20 more properties\n};\n\nit("gets user by id", () => {\n  // Only care about body.id but must fake entire Request\n  getUser({\n    body: { id: "123" },\n    headers: {},\n    cookies: {},\n    // ...fake all 20 properties\n  });\n});\n```\n\nAfter:\n\n```ts\nimport { fromPartial } from "@total-typescript/shoehorn";\n\nit("gets user by id", () => {\n  getUser(\n    fromPartial({\n      body: { id: "123" },\n    }),\n  );\n});\n```\n\n### `as Type` → `fromPartial()`\n\nBefore:\n\n```ts\ngetUser({ body: { id: "123" } } as Request);\n```\n\nAfter:\n\n```ts\nimport { fromPartial } from "@total-typescript/shoehorn";\n\ngetUser(fromPartial({ body: { id: "123" } }));\n```\n\n### `as unknown as Type` → `fromAny()`\n\nBefore:\n\n```ts\ngetUser({ body: { id: 123 } } as unknown as Request); // wrong type on purpose\n```\n\nAfter:\n\n```ts\nimport { fromAny } from "@total-typescript/shoehorn";\n\ngetUser(fromAny({ body: { id: 123 } }));\n```\n\n## When to use each\n\n| Function        | Use case                                           |\n| --------------- | -------------------------------------------------- |\n| `fromPartial()` | Pass partial data that still type-checks           |\n| `fromAny()`     | Pass intentionally wrong data (keeps autocomplete) |\n| `fromExact()`   | Force full object (swap with fromPartial later)    |\n\n## Workflow\n\n1. **Gather requirements** - ask user:\n   - What test files have `as` assertions causing problems?\n   - Are they dealing with large objects where only some properties matter?\n   - Do they need to pass intentionally wrong data for error testing?\n\n2. **Install and migrate**:\n   - [ ] Install: `npm i @total-typescript/shoehorn`. Detect package manager first: if `yarn.lock` exists use yarn, if `pnpm-lock.yaml` exists use pnpm, otherwise use npm.\n   - [ ] Find test files with `as` assertions: Use the Grep tool to find all `as Type` patterns: pattern `as [A-Z]`, glob `**/*.ts`.\n   - [ ] Replace `as Type` with `fromPartial()`\n   - [ ] Replace `as unknown as Type` with `fromAny()`\n   - [ ] Add imports from `@total-typescript/shoehorn`\n   - [ ] Run type check to verify\n',
  'request-refactor-plan':
    "This skill will be invoked when the user wants to create a refactor request. You should go through the steps below. You may skip steps if you don't consider them necessary.\n\n1. Ask the user for a long, detailed description of the problem they want to solve and any potential ideas for solutions.\n\n2. Explore the repo to verify their assertions and understand the current state of the codebase.\n\n3. Ask whether they have considered other options, and present other options to them.\n\n4. Interview the user about the implementation. Be extremely detailed and thorough.\n\n5. Hammer out the exact scope of the implementation. Work out what you plan to change and what you plan not to change.\n\n6. Look in the codebase to check for test coverage of this area of the codebase. If the code being refactored has no tests: write tests for the current behavior before creating the refactor plan. Do not proceed with the plan until tests exist — refactoring without tests means you cannot verify behavior is preserved.\n\n7. Break the implementation into a plan of tiny commits. Remember Martin Fowler's advice to \"make each refactoring step as small as possible, so that you can always see the program working.\" A tiny commit means: one logical change, all tests pass after it, the commit message describes the behavior change not the code change. Aim for commits reviewable in under 2 minutes. After each commit: run the full test suite. If tests fail, the commit is not done — fix the failure before making the next commit. Never commit with failing tests.\n\n8. Create a GitHub issue with the refactor plan. Use the following template for the issue description:\n\n<refactor-plan-template>\n\n## Problem Statement\n\nThe problem that the developer is facing, from the developer's perspective.\n\n## Solution\n\nThe solution to the problem, from the developer's perspective.\n\n## Commits\n\nA LONG, detailed implementation plan. Write the plan in plain English, breaking down the implementation into the tiniest commits possible. Each commit should leave the codebase in a working state.\n\n## Decision Document\n\nA list of implementation decisions that were made. This can include:\n\n- The modules that will be built/modified\n- The interfaces of those modules that will be modified\n- Technical clarifications from the developer\n- Architectural decisions\n- Schema changes\n- API contracts\n- Specific interactions\n\nDo NOT include specific file paths or code snippets. They may end up being outdated very quickly.\n\n## Testing Decisions\n\nA list of testing decisions that were made. Include:\n\n- A description of what makes a good test (only test external behavior, not implementation details)\n- Which modules will be tested\n- Prior art for the tests (i.e. similar types of tests in the codebase)\n\n## Out of Scope\n\nA description of the things that are out of scope for this refactor.\n\n## Further Notes (optional)\n\nAny further notes about the refactor.\n\n</refactor-plan-template>\n",
  'scaffold-domain':
    '# Scaffold Domain\n\nCreate the knowledge skeleton for one or more domains so that the FEATURE cycle\nskills have the correct files and structure to populate.\n\n## When to run\n\n- At project start, for all domains defined in `CLAUDE.md`\n- When a new domain is added mid-project\n\nSkip any domain that already has `backend/src/domains/<name>/AGENT_CONTEXT.md`.\n\n## Step 1 — Read CLAUDE.md\n\nRead `CLAUDE.md`. Confirm which domains need scaffolding from the Domains table.\n\nIf `CLAUDE.md` does not exist yet: create it using the template from `/initialize-knowledge-loop`, then continue.\n\n## Step 2 — For each domain, create these 3 files\n\n### `backend/src/domains/<name>/AGENT_CONTEXT.md`\n\n```markdown\n# <Domain Name> — Agent Context\n\n## Purpose\n_Populated by /update-agent-context after /grill-me._\n\n## Owned tables\n_Populated by /update-agent-context._\n- table_name — what it stores\n\n## File locations\n- router: backend/src/domains/<name>/<name>.router.ts\n- service: backend/src/domains/<name>/<name>.service.ts\n- repository: backend/src/domains/<name>/<name>.repository.ts\n\n## Glossary\n_Populated by /update-agent-context from grill-me session._\n\n## Architecture patterns\n_Populated as code is written._\n\n## Known debt\n_None._\n```\n\n### `backend/src/domains/<name>/domain-rules.yaml`\n\n```yaml\n# Business rules registry for <domain name> domain.\n# Rules are never deleted — status changes from active → superseded.\n# Rule ID format: RULE-<DOMAIN>-<NNN> (3-digit zero-padded integer)\n# Example: RULE-CATALOG-001\nrules: []\n```\n\n### `backend/src/domains/<name>/openapi.yaml`\n\n```yaml\nopenapi: "3.0.0"\ninfo:\n  title: <Domain Name> API\n  version: "0.0.0"\n  description: "Stub — updated as endpoints are built during TDD"\npaths: {}\ncomponents:\n  schemas: {}\n```\n\n## Step 3 — Update CLAUDE.md domain map\n\nFor each new domain scaffolded, use Edit tool to fill in the domain\'s row in `CLAUDE.md`:\n\n```\n| <name> | backend/src/domains/<name>/ | <purpose from CLAUDE.md> | — |\n```\n\nIf the domain row doesn\'t exist yet in CLAUDE.md, add it.\n\n## Step 4 — Confirm to user\n\nList every file created. State:\n\n> Domain scaffold complete for: [list of domains].\n> Next step: FEATURE cycle — run /grill-me → /update-agent-context → /write-a-prd → /break-into-tickets.\n',
  'scaffold-exercises':
    '# Scaffold Exercises\n\nCreate exercise directory structures that pass `pnpm ai-hero-cli internal lint`, then commit with `git commit`.\n\n## Directory naming\n\n- **Sections**: `XX-section-name/` inside `exercises/` (e.g., `01-retrieval-skill-building`)\n- **Exercises**: `XX.YY-exercise-name/` inside a section (e.g., `01.03-retrieval-with-bm25`)\n- Section number = `XX`, exercise number = `XX.YY`\n- Names are dash-case (lowercase, hyphens)\n\n## Exercise variants\n\nEach exercise needs at least one of these subfolders:\n\n- `problem/` - student workspace with TODOs\n- `solution/` - reference implementation\n- `explainer/` - conceptual material, no TODOs\n\nWhen stubbing, default to `explainer/` unless the plan specifies otherwise.\n\n## Required files\n\nEach subfolder (`problem/`, `solution/`, `explainer/`) needs a `readme.md` that:\n\n- Is **not empty** (must have real content, even a single title line works)\n- Has no broken links\n\nWhen stubbing, create a minimal readme with a title and a description:\n\n```md\n# Exercise Title\n\nDescription here\n```\n\nIf the subfolder has code, it also needs a `main.ts` (>1 line). But for stubs, a readme-only exercise is fine.\n\n## Workflow\n\n1. **Parse the plan** - extract section names, exercise names, and variant types\n2. **Create directories** - `mkdir -p` for each path\n3. **Create stub readmes** - one `readme.md` per variant folder with a title\n4. **Run lint** - `pnpm ai-hero-cli internal lint` to validate\n5. **Fix any errors** - iterate until lint passes\n\n## Lint rules summary\n\nThe linter (`pnpm ai-hero-cli internal lint`) checks:\n\n- Each exercise has subfolders (`problem/`, `solution/`, `explainer/`)\n- At least one of `problem/`, `explainer/`, or `explainer.1/` exists\n- `readme.md` exists and is non-empty in the primary subfolder\n- No `.gitkeep` files\n- No `speaker-notes.md` files\n- No broken links in readmes\n- No `pnpm run exercise` commands in readmes\n- `main.ts` required per subfolder unless it\'s readme-only\n\n## Moving/renaming exercises\n\nWhen renumbering or moving exercises:\n\n1. Use `git mv` (not `mv`) to rename directories - preserves git history\n2. Update the numeric prefix to maintain order\n3. Re-run lint after moves\n\nExample:\n\n```bash\ngit mv exercises/01-retrieval/01.03-embeddings exercises/01-retrieval/01.04-embeddings\n```\n\n## Example: stubbing from a plan\n\nGiven a plan like:\n\n```\nSection 05: Memory Skill Building\n- 05.01 Introduction to Memory\n- 05.02 Short-term Memory (explainer + problem + solution)\n- 05.03 Long-term Memory\n```\n\nCreate:\n\n```bash\nmkdir -p exercises/05-memory-skill-building/05.01-introduction-to-memory/explainer\nmkdir -p exercises/05-memory-skill-building/05.02-short-term-memory/{explainer,problem,solution}\nmkdir -p exercises/05-memory-skill-building/05.03-long-term-memory/explainer\n```\n\nThen create readme stubs:\n\n```\nexercises/05-memory-skill-building/05.01-introduction-to-memory/explainer/readme.md -> "# Introduction to Memory"\nexercises/05-memory-skill-building/05.02-short-term-memory/explainer/readme.md -> "# Short-term Memory"\nexercises/05-memory-skill-building/05.02-short-term-memory/problem/readme.md -> "# Short-term Memory"\nexercises/05-memory-skill-building/05.02-short-term-memory/solution/readme.md -> "# Short-term Memory"\nexercises/05-memory-skill-building/05.03-long-term-memory/explainer/readme.md -> "# Long-term Memory"\n```\n',
  'setup-pre-commit':
    '# Setup Pre-Commit Hooks\n\n## What This Sets Up\n\n- **Husky** pre-commit hook\n- **lint-staged** running Prettier on all staged files\n- **Prettier** config (if missing)\n- **typecheck** and **test** scripts in the pre-commit hook\n\n## Steps\n\n### 1. Detect package manager\n\nCheck for `package-lock.json` (npm), `pnpm-lock.yaml` (pnpm), `yarn.lock` (yarn), `bun.lockb` (bun). Use whichever is present. Default to npm if unclear.\n\n### 2. Install dependencies\n\nInstall as devDependencies:\n\n```\nhusky lint-staged prettier\n```\n\n### 3. Initialize Husky\n\n```bash\nnpx husky init\n```\n\nThis creates `.husky/` dir and adds `prepare: "husky"` to package.json.\n\n### 4. Create `.husky/pre-commit`\n\nWrite this file (no shebang needed for Husky v9+):\n\n```\nnpx lint-staged\nnpm run typecheck\nnpm run test\n```\n\n**Adapt**: Replace `npm` with detected package manager. If repo has no `typecheck` or `test` script in package.json, omit those lines and tell the user.\n\n### 5. Create `.lintstagedrc`\n\n```json\n{\n  "*": "prettier --ignore-unknown --write"\n}\n```\n\n### 6. Create `.prettierrc` (if missing)\n\nOnly create if no Prettier config exists. Use these defaults:\n\n```json\n{\n  "useTabs": false,\n  "tabWidth": 2,\n  "printWidth": 80,\n  "singleQuote": false,\n  "trailingComma": "es5",\n  "semi": true,\n  "arrowParens": "always"\n}\n```\n\n### 7. Verify\n\n- [ ] `.husky/pre-commit` exists and is executable\n- [ ] `.lintstagedrc` exists\n- [ ] `prepare` script in package.json is `"husky"`\n- [ ] `prettier` config exists\n- [ ] Run `npx lint-staged` to verify it works\n\n### 8. Commit\n\nStage all changed/created files and commit with message: `Add pre-commit hooks (husky + lint-staged + prettier)`\n\nThis will run through the new pre-commit hooks — a good smoke test that everything works.\n\n## Notes\n\n- Husky v9+ doesn\'t need shebangs in hook files\n- `prettier --ignore-unknown` skips files Prettier can\'t parse (images, etc.)\n- The pre-commit runs lint-staged first (fast, staged-only), then full typecheck and tests\n',
  'setup-spec-ci':
    '# Setup Spec CI\n\nInstall a CI job that continuously verifies every active rule in `domain-rules.yaml`\nhas at least one `test_cases` entry whose `input`/`expected` values are exercised\nby the actual test suite. Runs on every push and PR — not just at ship time.\n\n## When to run\n\nOnce, after Phase 4 (dev environment established) and before Phase 6 TDD begins.\nRe-run if you add a new domain or switch test frameworks.\n\n## Step 1 — Identify the test framework and test file locations\n\nRead the project root to detect:\n- `package.json` → look for `jest`, `vitest`, `mocha`, or `playwright` in devDependencies\n- `pytest.ini` / `pyproject.toml` → pytest\n- `go.mod` → Go test\n- Ask the user if none detected\n\nLocate test files:\n- JS/TS: `**/*.test.ts`, `**/*.spec.ts`, `**/__tests__/**`\n- Python: `tests/**/*.py`, `test_*.py`\n- Go: `**/*_test.go`\n\nRecord:\n- `TEST_CMD` — the command that runs all tests (e.g. `npm test`, `pytest`, `go test ./...`)\n- `TEST_GLOB` — the glob pattern for test files\n\n## Step 2 — Generate the spec-alignment script\n\nWrite `scripts/check-spec-alignment.sh` (or `.py` if project is Python-only):\n\n### Shell version (default)\n\n```bash\n#!/usr/bin/env bash\n# check-spec-alignment.sh\n# Verifies every active rule in domain-rules.yaml has test_cases present\n# and that each test_cases[].input string appears in at least one test file.\n# Exit 1 if any rule fails — use in CI and pre-push hooks.\n\nset -euo pipefail\n\nRULES_FILES=$(find . -name "domain-rules.yaml" -not -path "*/node_modules/*")\nTEST_GLOB="${TEST_GLOB:-**/*.test.ts **/*.spec.ts}"\nFAIL=0\n\nfor RULES_FILE in $RULES_FILES; do\n  DOMAIN=$(dirname "$RULES_FILE" | xargs basename)\n  echo "── Checking domain: $DOMAIN ($RULES_FILE)"\n\n  # Parse active rules with empty or missing test_cases\n  python3 - <<\'PYEOF\'\nimport yaml, sys, glob, os, re\n\nrules_file = os.environ.get(\'RULES_FILE\')\nwith open(rules_file) as f:\n    data = yaml.safe_load(f)\n\nrules = data.get(\'rules\', [])\ntest_glob_str = os.environ.get(\'TEST_GLOB\', \'**/*.test.ts\')\ntest_globs = test_glob_str.split()\n\n# Collect all test file contents once\ntest_bodies = []\nfor pattern in test_globs:\n    for path in glob.glob(pattern, recursive=True):\n        with open(path) as tf:\n            test_bodies.append(tf.read())\n\nfail = False\nfor rule in rules:\n    if rule.get(\'status\') not in (\'active\', \'draft\'):\n        continue\n    rid = rule.get(\'id\', \'?\')\n    tcs = rule.get(\'test_cases\', [])\n\n    if not tcs:\n        print(f\'  FAIL {rid}: no test_cases defined\')\n        fail = True\n        continue\n\n    for tc in tcs:\n        inp = str(tc.get(\'input\', \'\'))\n        if not inp or inp == \'None\':\n            print(f\'  FAIL {rid}: test_cases entry has no input value\')\n            fail = True\n            continue\n        found = any(inp in body for body in test_bodies)\n        if not found:\n            print(f\'  FAIL {rid}: test_cases input "{inp}" not found in any test file\')\n            fail = True\n        else:\n            print(f\'  OK   {rid}: "{inp[:40]}" found in tests\')\n\nsys.exit(1 if fail else 0)\nPYEOF\n  STATUS=$?\n  [ $STATUS -ne 0 ] && FAIL=1\ndone\n\nif [ $FAIL -ne 0 ]; then\n  echo ""\n  echo "spec-alignment FAILED — fix the above before merging."\n  echo "Run /tdd and fill test_cases in domain-rules.yaml for each rule."\n  exit 1\nfi\n\necho ""\necho "spec-alignment PASSED — all active rules have test coverage."\n```\n\nWrite to `scripts/check-spec-alignment.sh` and `chmod +x` it.\n\n## Step 3 — Generate the GitHub Actions workflow\n\nWrite `.github/workflows/spec-alignment.yml`:\n\n```yaml\nname: Spec Alignment\n\non:\n  push:\n    branches: ["**"]\n  pull_request:\n    branches: [main]\n\njobs:\n  spec-alignment:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Set up Python (for YAML parsing)\n        uses: actions/setup-python@v5\n        with:\n          python-version: "3.11"\n\n      - name: Install PyYAML\n        run: pip install pyyaml\n\n      - name: Run spec-alignment check\n        env:\n          TEST_GLOB: "**/*.test.ts **/*.spec.ts"   # adjust per project\n        run: bash scripts/check-spec-alignment.sh\n```\n\nIf the project uses Python tests, add the appropriate test setup steps before the spec-alignment check step.\n\nWrite to `.github/workflows/spec-alignment.yml`.\n\n## Step 4 — Add pre-push hook (optional but recommended)\n\nAsk the user: "Add a pre-push git hook so spec-alignment also runs locally before every push? (yes/no)"\n\nIf yes, write `.git/hooks/pre-push`:\n\n```bash\n#!/usr/bin/env bash\n# pre-push: run spec-alignment before every push\nbash scripts/check-spec-alignment.sh\n```\n\n`chmod +x .git/hooks/pre-push`.\n\nNote: `.git/hooks/` is not committed. Tell the user to add hook installation to their onboarding docs or `scripts/setup-dev.sh` so new contributors get it automatically.\n\n## Step 5 — Verify locally\n\nRun the script once now:\n\n```\nbash scripts/check-spec-alignment.sh\n```\n\nExpected outcomes:\n- **PASSED** — all active rules have test_cases with inputs found in test files. Proceed.\n- **FAILED with "no test_cases"** — run `/tdd` for the relevant rule and fill `test_cases` in domain-rules.yaml, then re-run.\n- **FAILED with "input not found in test files"** — the test_cases input strings in domain-rules.yaml don\'t match actual test file content. Update `test_cases[].input` to use the exact string that appears in the test (e.g. the function name, the describe block, or the specific value asserted).\n\nDo not commit the workflow until the script passes locally.\n\n## Step 6 — Commit\n\n```\ngit add scripts/check-spec-alignment.sh .github/workflows/spec-alignment.yml\ngit commit -m "Infra: add spec-alignment CI — domain-rules.yaml ↔ test suite enforcement"\n```\n\n## Step 7 — Confirm to user\n\nState:\n> Spec-alignment CI installed.\n> - scripts/check-spec-alignment.sh — run locally at any time\n> - .github/workflows/spec-alignment.yml — runs on every push and PR\n> - Pre-push hook: [installed / skipped]\n>\n> What this enforces: every active rule in domain-rules.yaml must have test_cases\n> with input values that appear in the real test suite. Fails CI if rules drift.\n>\n> Next step: Phase 6 TDD → /tdd\n',
  'ship-feature':
    '# Ship Feature\n\nAfter all tickets for a PRD are merged and tests pass, run this skill to update the\nknowledge layer and close the feature loop.\n\n## When to run\n\nAfter every ticket PR for a PRD is merged. Run once per PRD.\n\n## Step 0 — Pre-flight checks (HALT if any fail)\n\nRead `backend/src/domains/<name>/domain-rules.yaml` and verify:\n\n| Check | Condition | Failure action |\n|---|---|---|\n| No rules with `introduced_prd: TBD` or blank | All rules being shipped must have a PRD number | HALT — run /write-a-prd write-back first |\n| No active rules with empty `test_cases` | Every active rule must have at least one test case | HALT — run /tdd and fill test_cases first |\n| All ticket PRs merged | GitHub shows all checkboxes checked | HALT — merge remaining tickets first |\n\nIf all checks pass: proceed.\n\n## Step 1 — Gather context\n\nRead:\n1. The PRD GitHub issue: `gh issue view <issue-number>`\n2. `backend/src/domains/<name>/AGENT_CONTEXT.md`\n3. `backend/src/domains/<name>/domain-rules.yaml`\n4. `CLAUDE.md`\n\nIdentify from the PRD:\n- Feature name and issue number\n- Business Rules Affected section (which rule IDs were changed)\n- Domains involved\n\n**Divergence check:** Compare the PRD\'s described feature scope with what the code actually implements. If they differ:\n- Flag: "PRD says X, code does Y"\n- Ask the user:\n  - A) Update the PRD to reflect what was built — then proceed\n  - B) Revert code to match PRD — do not ship until code matches\n  - C) Ship as-built and note the divergence in the PRD issue\n- Wait for user choice before continuing.\n\n## Step 2 — Promote draft rules to active\n\nFor each rule in the PRD\'s "Business Rules Affected" section:\n\nRun `/validate-knowledge` only if any rule is being superseded or a new rule is being added. If all rules are already active with the correct `introduced_prd`, skip.\n\nIf APPROVED:\n- Rules with `status: draft` → set `status: active`\n- Fill `introduced_prd: <issue number>` for any rules missing it\n- Rules being superseded: set `status: superseded`, `superseded_by: <new-rule-id>`\n- New rules replacing old ones: add as `status: active`, `supersedes: <old-rule-id>`\n\nUse Edit tool on domain-rules.yaml.\n\n## Step 3 — Update AGENT_CONTEXT.md\n\nFor the domain(s) involved, use Edit tool to update:\n- **Architecture patterns** — if new patterns were introduced\n- **Known debt** — if TDD surfaced debt\n- **File locations** — if new files were added\n- **Owned tables** — if schema changed\n- **Ticket handoffs** — clear the entire `## Ticket handoffs` section now that all PRs for this PRD are merged. Delete from the `## Ticket handoffs` heading to the end of that section (all `### Ticket N` entries). The handoff notes are no longer needed — they were for in-flight coordination only.\n\nDo not rewrite the whole file — only update sections that changed.\n\n## Step 4 — Update CLAUDE.md\n\nFor any seam that was established during this feature:\n- Update status from `Not yet established` to `Active — established in PRD #<N>`\n\nFor any capability or domain that changed status:\n- Update the Domains table row if the purpose description changed\n\nUse Edit tool.\n\n## Step 5 — Close the PRD issue\n\n```\ngh issue edit <number> --add-label "status: shipped"\ngh issue close <number> --comment "Shipped. All <N> tickets merged. Rules promoted to active."\n```\n\n## Step 6 — Create git commit\n\n```\ngit add backend/src/domains/<name>/AGENT_CONTEXT.md\ngit add backend/src/domains/<name>/domain-rules.yaml\ngit add CLAUDE.md\ngit commit -m "Ship: <feature-name> — rules active, context updated"\n```\n\n## Step 7 — Confirm to user\n\nState:\n> Feature shipped: <name>\n> - domain-rules.yaml: <N> rules promoted to active\n> - AGENT_CONTEXT.md: updated (patterns, debt, tables)\n> - CLAUDE.md: seam statuses updated\n> - PRD #<N>: closed with label "status: shipped"\n> Next step: FEATURE cycle for next PRD.\n',
  tdd: "# Test-Driven Development\n\n## Scope — read this first\n\nYou are working on **one ticket** from a PRD. Before writing any code:\n\n1. Read the PRD: `gh issue view <issue-number>`\n2. Read `CLAUDE.md` — identify the domain and load the context-loading instructions\n3. Read `backend/src/domains/<name>/AGENT_CONTEXT.md`\n4. Read `backend/src/domains/<name>/domain-rules.yaml`\n5. If the ticket touches a cross-domain seam: read `contracts/<provider>-to-<consumer>/CONTRACT.md`\n\nYour ticket scope is the single checkbox you were given. **Do not implement other tickets.** If you discover that your ticket depends on work that isn't merged yet, stop and tell the user.\n\n---\n\n## Philosophy\n\n**Core principle**: Tests should verify behavior through public interfaces, not implementation details. Code can change entirely; tests shouldn't.\n\n**Good tests** are integration-style: they exercise real code paths through public APIs. They describe _what_ the system does, not _how_ it does it. A good test reads like a specification — \"user can checkout with valid cart\" tells you exactly what capability exists. These tests survive refactors because they don't care about internal structure.\n\n**Bad tests** are coupled to implementation. They mock internal collaborators, test private methods, or verify through external means (like querying a database directly instead of using the interface). Warning sign: your test breaks when you refactor, but behavior hasn't changed.\n\nSee [tests.md](tdd-refs/tests.md) and [mocking.md](tdd-refs/mocking.md) for examples.\n\n---\n\n## Anti-Pattern: Horizontal Slices\n\n**DO NOT write all tests first, then all implementation.**\n\nThis produces crap tests: written in bulk, they test imagined behavior, not actual behavior. You end up testing the shape of things rather than user-facing behavior.\n\n**Correct approach**: Vertical slices via tracer bullets. One test → one implementation → repeat.\n\n```\nWRONG (horizontal):\n  RED:   test1, test2, test3, test4, test5\n  GREEN: impl1, impl2, impl3, impl4, impl5\n\nRIGHT (vertical):\n  RED→GREEN: test1→impl1\n  RED→GREEN: test2→impl2\n  ...\n```\n\n---\n\n## Workflow\n\n### 1. Planning\n\nBefore writing any code:\n\n- [ ] Confirm which behaviors to test for this ticket (from the PRD user stories)\n- [ ] Check domain-rules.yaml — which rules does this ticket exercise?\n- [ ] Confirm the interface changes needed\n- [ ] List the behaviors to test (not implementation steps)\n- [ ] Get user approval on the plan\n\n### 2. Tracer Bullet\n\nWrite ONE test that confirms ONE thing:\n\n```\nRED:   Write test for first behavior → test fails\nGREEN: Write minimal code to pass → test passes\n```\n\n### 3. Incremental Loop\n\nFor each remaining behavior:\n\n```\nRED:   Write next test → fails\nGREEN: Minimal code to pass → passes\n```\n\nRules:\n- One test at a time\n- Only enough code to pass the current test\n- Don't anticipate future tests\n- Tests focus on observable behavior\n\n### 4. Fill test_cases in domain-rules.yaml\n\nAfter each rule's test passes, update that rule's `test_cases` in domain-rules.yaml:\n\n```yaml\ntest_cases:\n  - input: \"<the exact string or value that appears in the test>\"\n    expected: \"<what the test asserts>\"\n```\n\nUse Edit tool — never overwrite the file.\n\n### 5. Refactor\n\nAfter all tests pass:\n\n- [ ] Extract duplication\n- [ ] Deepen modules (complexity behind simple interfaces)\n- [ ] Run tests after each refactor step\n\n**Never refactor while RED.**\n\n### 6. Update openapi.yaml\n\nIf this ticket added or changed API endpoints, update `backend/src/domains/<name>/openapi.yaml` to match.\n\n### 7. Write handoff note to AGENT_CONTEXT.md\n\nBefore opening the PR, append a brief handoff note under `## Ticket handoffs` in `backend/src/domains/<name>/AGENT_CONTEXT.md`. If the section does not exist, add it at the end of the file.\n\nWrite only what a subsequent ticket in this same PRD needs to know — interfaces created, design decisions made during coding that are not in the PRD, error types introduced. Keep it to 3–5 bullet points.\n\n```markdown\n## Ticket handoffs\n<!-- Cleared by /ship-feature after all PRs for this PRD are merged -->\n\n### Ticket N — <description> (PRD #M)\n- <Module> created: <interface summary, input/output types, key constraints>\n- <Design decision made during implementation not captured in PRD or AGENT_CONTEXT.md>\n- <Error type introduced, e.g. throws InvalidLineItemError if unit_price_cents < 0>\n```\n\nDo NOT include: implementation details, things already in AGENT_CONTEXT.md, things in domain-rules.yaml, or things the next ticket can read from the code itself.\n\nUse Edit tool — append only, never overwrite.\n\n---\n\n## Stop Condition\n\nStop when: all behaviors described in your ticket's checkbox have at least one passing test, openapi.yaml is updated, and the handoff note is written. Open a PR. Do not implement the next ticket.\n\n## Checklist Per Cycle\n\n```\n[ ] Test describes behavior, not implementation\n[ ] Test uses public interface only\n[ ] Test would survive internal refactor\n[ ] Code is minimal for this test\n[ ] No speculative features added\n[ ] domain-rules.yaml test_cases filled for rules exercised\n[ ] Handoff note written to AGENT_CONTEXT.md ## Ticket handoffs\n```\n",
  'triage-issue':
    "# Triage Issue\n\nInvestigate a reported problem, find its root cause, and create a GitHub issue with a TDD fix plan. This is a mostly hands-off workflow - minimize questions to the user.\n\n## Process\n\n### 1. Capture the problem\n\nGet a brief description of the issue from the user. If they haven't provided one, ask ONE question: \"What's the problem you're seeing?\"\n\nDo NOT ask follow-up questions yet. Start investigating immediately.\n\n### 2. Explore and diagnose\n\nUse the Agent tool with subagent_type=Explore to deeply investigate the codebase. Your goal is to find:\n\n- **Where** the bug manifests (entry points, UI, API responses)\n- **What** code path is involved (trace the flow)\n- **Why** it fails (the root cause, not just the symptom)\n- **What** related code exists (similar patterns, tests, adjacent modules)\n\nLook at:\n- Related source files and their dependencies\n- Existing tests (what's tested, what's missing)\n- Recent changes to affected files (`git log` on relevant files)\n- Error handling in the code path\n- Similar patterns elsewhere in the codebase that work correctly\n\n### 3. Identify the fix approach\n\nBased on your investigation, determine:\n\n- The minimal change needed to fix the root cause. Minimal means: does not refactor surrounding code, does not add features, does not change interfaces. If the minimal fix is wrong (e.g. would require changing an interface to be correct), document this explicitly in the issue as \"minimal fix is insufficient — interface change required\" and propose the interface change as a separate issue.\n- Which modules/interfaces are affected\n- What behaviors need to be verified via tests\n- Whether this is a regression, missing feature, or design flaw\n\nIf the fix touches `domain-rules.yaml` or changes business behavior: this is not a bug fix, it is a PRD change. Stop, create a PRD instead with /write-a-prd.\n\n### 4. Design TDD fix plan\n\nCreate a concrete, ordered list of RED-GREEN cycles. Each cycle is one vertical slice:\n\n- **RED**: Describe a specific test that captures the broken/missing behavior\n- **GREEN**: Describe the minimal code change to make that test pass\n\nRules:\n- Tests verify behavior through public interfaces, not implementation details\n- One test at a time, vertical slices (NOT all tests first, then all code)\n- Each test should survive internal refactors\n- Include a final refactor step if needed\n- **Durability**: Only suggest fixes that would survive radical codebase changes. A fix survives radical changes if it: (1) does not hardcode file paths or function names that could be refactored, (2) targets behavior rather than implementation, (3) would remain correct if the surrounding module was rewritten. A fix that references more than 2 files outside the bug's immediate module is a signal the fix is too broad. Describe behaviors and contracts, not internal structure. Tests assert on observable outcomes (API responses, UI state, user-visible effects), not internal state. A good suggestion reads like a spec; a bad one reads like a diff.\n\n### 5. Create the GitHub issue\n\nLabel the issue: 'bug' for behavioral defects, 'fix' for incorrect implementation that matches spec, 'debt' for cases where the spec itself was ambiguous. Add a 'domain:<name>' label.\n\nCreate a GitHub issue using `gh issue create` with the template below. Do NOT ask the user to review before creating - just create it and share the URL.\n\n<issue-template>\n\n## Problem\n\nA clear description of the bug or issue, including:\n- What happens (actual behavior)\n- What should happen (expected behavior)\n- How to reproduce (if applicable)\n\n## Root Cause Analysis\n\nDescribe what you found during investigation:\n- The code path involved\n- Why the current code fails\n- Any contributing factors\n\nDo NOT include specific file paths, line numbers, or implementation details that couple to current code layout. Describe modules, behaviors, and contracts instead. The issue should remain useful even after major refactors.\n\n## TDD Fix Plan\n\nA numbered list of RED-GREEN cycles:\n\n1. **RED**: Write a test that [describes expected behavior]\n   **GREEN**: [Minimal change to make it pass]\n\n2. **RED**: Write a test that [describes next behavior]\n   **GREEN**: [Minimal change to make it pass]\n\n...\n\n**REFACTOR**: [Any cleanup needed after all tests pass]\n\n## Acceptance Criteria\n\n- [ ] Criterion 1\n- [ ] Criterion 2\n- [ ] All new tests pass\n- [ ] Existing tests still pass\n\n</issue-template>\n\nAfter creating the issue, print the issue URL and a one-line summary of the root cause.\n",
  'update-agent-context':
    '# Update Agent Context\n\nTranslate the output of `/grill-me` (which lives in the current conversation) into an\nupdated `AGENT_CONTEXT.md` and seed any business rules discovered into `domain-rules.yaml`.\n\n## When to run\n\nAfter `/grill-me` completes, before `/write-a-prd`.\n\n## Step 1 — Identify the domain\n\nFrom the grill-me conversation, identify which domain this feature belongs to.\n\nRead:\n1. `CLAUDE.md` — confirm the domain exists in the domain map\n2. `backend/src/domains/<name>/AGENT_CONTEXT.md` — existing content (check what\'s already there)\n3. `backend/src/domains/<name>/domain-rules.yaml` — existing rules\n\nIf `AGENT_CONTEXT.md` does not exist yet, run `/scaffold-domain` first, then return here.\n\n## Step 2 — Update AGENT_CONTEXT.md\n\nThe file follows a fixed template. Update only the sections where grill-me produced new information. Do not blank out sections that are already populated.\n\nSections to update:\n- **Purpose** — if the grill-me session clarified or refined the domain\'s purpose\n- **Owned tables** — if new entities were identified\n- **File locations** — if new files were identified\n- **Glossary** — add every domain-specific term that came up in the grill-me session. Format: `Term: definition`.\n- **Architecture patterns** — if patterns were confirmed or changed\n- **Known debt** — if the grill-me session surfaced technical debt relevant to this feature\n\nUse Edit tool — never overwrite the file.\n\n## Step 3 — Seed business rules to domain-rules.yaml\n\nFor each business rule surfaced during grill-me that is not already in domain-rules.yaml:\n\nRun `/validate-knowledge` with:\n- Incoming: the rule\'s plain_english and formula\n- Domain: this domain name\n- Target: domain-rules.yaml\n\nIf APPROVED, add the rule as `status: draft`:\n\n```yaml\n- id: RULE-<DOMAIN>-<NNN>\n  status: draft\n  plain_english: >\n    <verbose description — see validate-knowledge YAML spec>\n  formula: "<calculation or condition, if applicable>"\n  why: "<business rationale>"\n  entity: <entity>\n  field: <field>\n  operation: <operation>\n  introduced_prd: TBD\n  test_cases: []\n```\n\nRule ID format: `RULE-<DOMAIN>-<NNN>` where NNN is a zero-padded 3-digit integer.\nFind the next ID by reading domain-rules.yaml and incrementing the highest NNN for this domain.\n\nIf BLOCKED by validate-knowledge: stop, report to user, do not write the rule.\n\n## Step 4 — Confirm to user\n\nState:\n> AGENT_CONTEXT.md updated for domain: <name>\n> Glossary terms added: <N>\n> Draft rules seeded: <N> (or "none — no new rules surfaced")\n> Next step: /write-a-prd\n',
  'validate-knowledge':
    '# Validate Knowledge\n\nA quality gate that detects overlapping business rules using cheap LLM text comparison against existing rule text — not key lookup.\n\nThis skill **always gates** writes to `domain-rules.yaml`. It **optionally gates** writes to `AGENT_CONTEXT.md` — only when the incoming content contradicts or overlaps with existing rules in `domain-rules.yaml`. It does **not** gate routine context updates, formatting changes, stub creation, or any write that adds new content without touching business rules.\n\n## When to run\n\nCalled by other skills before they write to knowledge docs:\n- `/update-agent-context` → always, before seeding draft rules to domain-rules.yaml; not required for prose sections of AGENT_CONTEXT.md\n- `/ship-feature` → only when superseding an existing rule or adding a new rule to domain-rules.yaml; skip if rules are already active with no changes\n\nCan also be invoked directly: "validate-knowledge before updating billing rules"\n\n## Input required\n\n1. **Incoming rule text** — plain_english + formula extracted from the PRD being processed\n2. **Domain name** — which domain\'s rules to scan\n3. **Target file path** — which knowledge doc is being updated\n\n## Process\n\n### Step 1 — Extract incoming rule from PRD\n\nRead the PRD and extract:\n- `plain_english` — verbose description of what the rule governs (see YAML spec below)\n- `formula` — the calculation or condition if applicable\n- `why` — business rationale\n\n### Step 2 — Cheap LLM text scan against domain-rules.yaml\n\nRead the full `domain-rules.yaml` for this domain. Use the cheapest available model (Haiku or equivalent) for this scan — this is a text comparison task, not a reasoning task. For each **active** or **draft** rule in the file, ask:\n\n> "Do these two rules govern the same business decision, even partially? Rule A: [incoming plain_english + formula]. Rule B: [existing plain_english + formula]. Answer YES or NO with one sentence of reasoning."\n\nSuperseded and retired rules are skipped — they are history.\n\nThis is the primary overlap detection mechanism. No conflict_key, no entity/field/operation matching.\n\n**Prompt discipline:** The question must be "same business decision" not "similar topic". Two rules about loyalty points that cover non-overlapping conditions (VIP vs non-VIP) are NOT the same decision. The LLM reasoning sentence is surfaced to the human in all non-CLEAN outcomes.\n\n### Step 3 — If no overlap found → CLEAN\n\nNo existing rule covers the same business decision as the incoming rule.\n→ Auto-write. No PRD fetch. No human gate.\n\n### Step 4 — If overlap found → check if intent is clear from incoming PRD alone\n\nYou already have the incoming PRD open (you extracted the rule text from it in Step 1).\nBefore fetching anything else, read its full text and ask:\n\n> Does this PRD state what it intends to do with the overlapping rule?\n> Examples of clear intent: "replaces our current earn rate", "adds a weekend modifier on top of the existing rule", "this feature is being removed", "this is a VIP-only variant".\n\nIf intent is clear → **DECLARED-WRITE** (see Step 5). No second fetch needed.\n\nIf intent is not stated or ambiguous → fetch the historical PRD:\n\n```\n📥 fetch historical PRD  gh issue view <introduced_prd of overlapping rule>\n```\n\nRead both PRDs. Determine intent.\n\n### Step 5 — Classify\n\n**DECLARED-WRITE** — the incoming PRD text clearly states the relationship to the overlapping rule (replaces it, layers on it, scopes it, retires it). No rule IDs needed — plain English intent is sufficient.\n→ Auto-execute. No second fetch. PM already made the decision when writing the PRD.\n\n**INFERRED-WRITE** — no explicit declaration, but reading both PRDs makes intent unambiguous (one clearly replaces or layers on the other).\n→ Auto-write with reasoning shown. No human gate.\n\n**SCOPE-INFERRED** — overlap exists but incoming rule has a narrower scope (e.g. VIP only) that forces annotation on an existing rule the current PRD does not own.\n→ BLOCKED. Human must authorise modification to a rule they did not author.\n\n**CONFLICT-UNRESOLVABLE** — both PRDs assert contradictory product intent with no clear resolution path.\n→ BLOCKED. Human makes product decision. No write until resolved.\n\n### Step 6 — Chain check (for all auto paths)\n\nBefore writing any DECLARED-WRITE or INFERRED-WRITE, scan all domain-rules.yaml files for rules with `depends_on` pointing to the rule being changed.\n\nIf a rule has a `depends_on` reference pointing to a rule that no longer exists in any domain-rules.yaml, flag it separately as **DATA-INTEGRITY** and block the write until the `depends_on` is corrected. Do not proceed with the write until the broken reference is fixed.\n\nIf any found (and the referenced rule does exist) → escalate to **CHAIN-IMPACT**:\n```\n⚠ CHAIN-IMPACT\n  Rule 1.2.1 depends_on 1.1.2 which is being superseded.\n  Human must confirm: does 1.2.1 still compose correctly with incoming rule?\n```\n\n### Step 7 — Show diff and wait for approval\n\nFor all auto paths (CLEAN, DECLARED-WRITE, INFERRED-WRITE), show exact diff before writing:\n\n```\nAPPROVED — validate-knowledge-[OUTCOME]\n\nChanges to be written:\n  [SUPERSEDE] 1.1.2 → status: superseded, superseded_by: 1.1.3\n  [NEW] 1.1.3 — earn points on net total after discount, tier multiplied\n\nProceed? (yes to write / no to cancel)\n```\n\nWait for explicit "yes" before any write.\n\nFor BLOCKED outcomes:\n\n```\nBLOCKED — validate-knowledge-[OUTCOME]\n\nIncoming rule: [plain_english]\n\nOverlaps with existing rule 1.1.2:\n  [plain_english of existing rule]\n  LLM reasoning: [one sentence from Step 2 scan]\n\nResolution options:\n  A) Supersede 1.1.2 (incoming PRD changes this rule)\n  B) These are different rules — rename incoming to avoid overlap\n  C) Cancel — investigate before proceeding\n\nChoose A, B, or C:\n```\n\nDo not proceed until human makes a choice.\n\n## YAML spec — what makes a good plain_english field\n\nThe `plain_english` field is the primary matching surface. It must be verbose enough that a cheap LLM can confidently judge overlap. Bad: "1 point per dollar". Good:\n\n```\n"Customers earn loyalty points on every completed order. The earning rate is 1 point\nper whole dollar of the order total (net of discounts), calculated as\nfloor(net_total_cents / 100). Fractional dollars do not earn partial points — they\nare discarded. This rule applies to all customer tiers (Bronze, Silver, Gold) and\nall order types except gift card purchases. It does not apply to cancelled or\nrefunded orders. The intent is a simple, predictable rate that customers can\ncalculate mentally."\n```\n\nWhen writing a new rule to YAML, always write plain_english at this level of detail.\n\n## Rules for the skill itself\n\n- Always run this check before any write to domain-rules.yaml\n- Run this check before writing to AGENT_CONTEXT.md only when the incoming content contradicts or overlaps with existing rules — not for routine context updates\n- Never auto-resolve a conflict — always surface it to the human\n- A human "yes" to the APPROVED diff is required before any write\n- Use the Edit tool for all writes — never overwrite entire files\n- Never delete rules — only change status to superseded or retired\n',
  'write-a-prd':
    'This skill will be invoked when the user wants to create a PRD. You may skip steps if you don\'t consider them necessary.\n\n1. Ask the user for a long, detailed description of the problem they want to solve and any potential ideas for solutions.\n\n2. Explore the repo to verify their assertions and understand the current state of the codebase.\n\n3. Interview the user relentlessly about every aspect of this plan until you reach a shared understanding. Walk down each branch of the design tree, resolving dependencies between decisions one-by-one.\n\n4. Sketch out the major modules you will need to build or modify to complete the implementation. Actively look for opportunities to extract deep modules that can be tested in isolation.\n\nA deep module (as opposed to a shallow module) is one which encapsulates a lot of functionality in a simple, testable interface which rarely changes.\n\nRun /design-an-interface for any module where the API shape is unclear. Do not guess at interface shapes in the PRD — run the design skill first and reference the chosen design.\n\nCheck with the user that these modules match their expectations. Check with the user which modules they want tests written for.\n\n5. Once you have a complete understanding of the problem and solution, use the template below to write the PRD. The PRD should be submitted as a GitHub issue.\n\n6. **Immediately after the issue is submitted** — write back `introduced_prd` to `domain-rules.yaml`:\n   - Read `backend/src/domains/<name>/docs/domain-rules.yaml`\n   - For every rule ID listed in the PRD\'s "Business Rules Affected" section with `action: add` or `action: supersede`, set `introduced_prd: <issue number>`\n   - Rule IDs follow the canonical format `RULE-<DOMAIN>-<NNN>` where NNN is a zero-padded 3-digit integer (e.g. RULE-CATALOG-001). Never use decimal sub-IDs like 001.1 — those are not valid. To find the next ID: read `domain-rules.yaml`, find the highest NNN for this domain, add 1. Write `introduced_prd` using Edit tool — never overwrite the file.\n   - Use Edit tool — do not overwrite any other fields\n   - This closes the traceability loop: PRD ↔ rules are bidirectionally linked from the moment the PRD is submitted\n\n<prd-template>\n\n## Problem Statement\n\nThe problem that the user is facing, from the user\'s perspective.\n\n## Solution\n\nThe solution to the problem, from the user\'s perspective.\n\n## Workflow Actors\n\nDerived from the grill-me Workflow Actors branch. List every actor in this feature\'s workflows.\n\n| Actor | Type | What they own | Autonomous decisions | Escalates to human when |\n|---|---|---|---|---|\n| Example: Fulfillment Agent | AI agent | Routes orders to warehouse | Stock available + address valid | Ambiguous address, no stock anywhere, high-value order |\n| Example: Customer | Human | Places and tracks orders | — | — |\n\nIf no AI agents are involved, write: "No AI agents in this feature — all actors are humans or automated systems."\n\n## User Stories\n\nA LONG, numbered list of user stories. Each user story should be in the format of:\n\n1. As an <actor>, I want a <feature>, so that <benefit>\n\nActors may be human roles, automated systems, or AI agents surfaced in the Workflow Actors section above. Use the canonical agent name from the AGENT_CONTEXT.md Glossary section.\n\n<user-story-example>\n1. As a mobile bank customer, I want to see balance on my accounts, so that I can make better informed decisions about my spending\n2. As a Fraud Detection Agent, I want to flag transactions over $5,000 for human review, so that suspicious activity is caught before funds are released\n</user-story-example>\n\nThis list of user stories should be extremely extensive and cover all aspects of the feature.\n\n## Implementation Decisions\n\nA list of implementation decisions that were made. This can include:\n\n- The modules that will be built/modified\n- The interfaces of those modules that will be modified\n- Technical clarifications from the developer\n- Architectural decisions\n- Schema changes\n- API contracts\n- Specific interactions\n\nDo NOT include specific file paths or code snippets. They may end up being outdated very quickly.\n\n## Testing Decisions\n\nA list of testing decisions that were made. Include:\n\n- A description of what makes a good test (only test external behavior, not implementation details)\n- Which modules will be tested\n- Prior art for the tests (i.e. similar types of tests in the codebase)\n\n## Business Rules Affected\n\nA structured list of business rules this PRD changes, adds, or leaves untouched.\nUse rule IDs from `domain-rules.yaml`. This section is required — it is used by\n`/validate-knowledge` and `/ship-feature` for deterministic conflict detection.\n\nFormat:\n```\nchanges:\n  - rule_id: RULE-<DOMAIN>-001\n    action: supersede\n    new_rule: "<new rule in one sentence>"\n    reason: "<why this rule is changing>"\n\n  - rule_id: RULE-<DOMAIN>-002\n    action: add\n    new_rule: "<new rule in one sentence>"\n    entity: <entity>\n    field: <field>\n    operation: <operation>\n    reason: "<why this rule is being added>"\n\n  - rule_id: RULE-<DOMAIN>-003\n    action: no_change\n    reason: "<confirm this rule is not affected>"\n```\n\nIf this PRD introduces business rules where no rule IDs exist yet, write `new` in\nplace of the rule_id — `/update-agent-context` will assign the ID.\n\n## Out of Scope\n\nA description of the things that are out of scope for this PRD.\n\n## Further Notes\n\nAny further notes about the feature.\n\n</prd-template>\n',
  'write-a-skill':
    '# Writing Skills\n\n## Process\n\n1. **Gather requirements** - ask user about:\n   - What task/domain does the skill cover?\n   - What specific use cases should it handle?\n   - Does it need executable scripts or just instructions?\n   - Any reference materials to include?\n\n2. **Draft the skill** - create:\n   - SKILL.md with concise instructions\n   - Additional reference files if content exceeds 500 lines\n   - Utility scripts if deterministic operations needed\n\n3. **Review with user** - present draft and ask:\n   - Does this cover your use cases?\n   - Anything missing or unclear?\n   - Should any section be more/less detailed?\n\n## Skill Structure\n\n```\nskill-name/\n├── SKILL.md           # Main instructions (required)\n├── REFERENCE.md       # Detailed docs (if needed)\n├── EXAMPLES.md        # Usage examples (if needed)\n└── scripts/           # Utility scripts (if needed)\n    └── helper.js\n```\n\n## SKILL.md Template\n\n```md\n---\nname: skill-name\ndescription: Brief description of capability. Use when [specific triggers].\n---\n\n# Skill Name\n\n## Quick start\n\n[Minimal working example]\n\n## Workflows\n\n[Step-by-step processes with checklists for complex tasks]\n\n## Advanced features\n\n[Link to separate files: See [REFERENCE.md](REFERENCE.md)]\n```\n\n## Description Requirements\n\nThe description is **the only thing your agent sees** when deciding which skill to load. It\'s surfaced in the system prompt alongside all other installed skills. Your agent reads these descriptions and picks the relevant skill based on the user\'s request.\n\n**Goal**: Give your agent just enough info to know:\n\n1. What capability this skill provides\n2. When/why to trigger it (specific keywords, contexts, file types)\n\n**Format**:\n\n- Max 1024 chars\n- Write in third person\n- First sentence: what it does\n- Second sentence: "Use when [specific triggers]"\n\n**Good example**:\n\n```\nExtract text and tables from PDF files, fill forms, merge documents. Use when working with PDF files or when user mentions PDFs, forms, or document extraction.\n```\n\n**Bad example**:\n\n```\nHelps with documents.\n```\n\nThe bad example gives your agent no way to distinguish this from other document skills.\n\n## When to Add Scripts\n\nAdd utility scripts when:\n\n- Operation is deterministic (validation, formatting)\n- Same code would be generated repeatedly\n- Errors need explicit handling\n\nScripts save tokens and improve reliability vs generated code.\n\n## Skill Scope\n\nA skill should encapsulate one trigger and one outcome. If you can describe when to stop the skill without it feeling arbitrary, it\'s the right scope. If the skill has two natural stopping points, split it. Rule of thumb: if the user could reasonably want only the first half, it\'s two skills.\n\n## When to Split Files\n\nSplit into separate files when:\n\n- SKILL.md exceeds 100 lines\n- Content has distinct domains (finance vs sales schemas)\n- Advanced features are rarely needed\n\n## Review Checklist\n\nAfter drafting, verify:\n\n- [ ] Description includes triggers ("Use when...")\n- [ ] SKILL.md under 100 lines\n- [ ] No time-sensitive info\n- [ ] Consistent terminology\n- [ ] Concrete examples included\n- [ ] References one level deep\n',
}
