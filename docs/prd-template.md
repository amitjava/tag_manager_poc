# PRD: [Feature Name]

> Status: Draft
> Created: YYYY-MM-DD
> Skill used: /write-a-prd

---

## 1. Feature Overview

One paragraph. What is being built and why it matters at a product level.

---

## 2. Problem Statement

What pain does this solve? Who experiences it and how often?

---

## 3. Solution

How does this feature solve the problem? One paragraph, user-visible only — no implementation details.

---

## 4. Ubiquitous Language

These terms are canonical across the database, API, and UI. Do not substitute synonyms.

| Term      | Definition |
| --------- | ---------- |
| **Term1** | Definition |
| **Term2** | Definition |

---

## 5. User Stories

Behavioral stories carry a `[rule: N.N.N]` badge. UI/layout stories have no badge.

1. As a [user], I want [behaviour] so that [outcome]. `[rule: N.N.N]`
2. As a [user], I want [UI behaviour] so that [outcome].
3. As a [user], I want [behaviour] so that [outcome]. `[rule: N.N.N]`

---

## 6. Acceptance Criteria

Behavioral items carry a `[N.N.N]` badge. UI/layout items stay as plain prose.

### [Feature Area 1]

- [ ] [Behavioural criterion] `[N.N.N]`
- [ ] [UI/layout criterion]
- [ ] [Behavioural criterion] `[N.N.N]`

### [Feature Area 2]

- [ ] [Behavioural criterion] `[N.N.N]`
- [ ] [UI/layout criterion]

---

## 7. Implementation Decisions

### Database

- Tables, columns, constraints.

### API

| Method | Route               | Description |
| ------ | ------------------- | ----------- |
| GET    | `/api/resource`     | List all    |
| POST   | `/api/resource`     | Create      |
| PUT    | `/api/resource/:id` | Update      |
| DELETE | `/api/resource/:id` | Delete      |

### Domain

**Domain:** `domain-name`
All modules live under `backend/src/domains/<domain-name>/`.

### Modules

- **Repository** — all DB read/write operations.
- **Service** — business logic.
- **Controller** — route handlers, delegates to Service/Repository.

### Frontend

- Routing, UI components, data fetching approach.

---

## 8. Testing Decisions

### What to test

**[Module]** (unit)

- [test case 1]
- [test case 2]

**API endpoints** (integration)

- `POST /api/resource` — 201 on success, 400 on validation failure
- `GET /api/resource` — 200 with array
- `DELETE /api/resource/:id` — 200 on success, 404 if not found

---

## 9. Tracer Bullet Slices

| Slice       | What it covers | Done when            |
| ----------- | -------------- | -------------------- |
| **Slice 1** | [scope]        | [observable outcome] |
| **Slice 2** | [scope]        | [observable outcome] |
| **Slice 3** | [scope]        | [observable outcome] |

---

## 10. Tickets

Generated by `/break-into-tickets`. One checkbox = one Claude window = one PR.

- [ ] Ticket 1: [scope]
- [ ] Ticket 2: [scope]
- [ ] Ticket 3: [scope]

---

## 11. Out of Scope

- [item]
- [item]

---

## 12. Open Questions

None. All questions resolved during `/grill-me` session.
See: [grill-me-qa-log.md](./grill-me-qa-log.md)

---

## 13. Further Notes

Any project-specific guidance, constraints, or context for the executing Claude window.
